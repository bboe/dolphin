<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#"><%= ENV['APP_TITLE'] || 'Dolphin' %></a>
    </div>

    <ul class="nav navbar-nav navbar-right">
      <li class="dropdown">
        <a href="#" class="dropdown-toggle no-pad" data-toggle="dropdown" role="button" aria-expanded="false">
          <img src="<%= current_user.image_url %>">
          <%= current_user.name %><span class="caret"></span>
        </a>
        <ul class="dropdown-menu" role="menu">
            <li><%= link_to "Re-Authenticate", user_omniauth_authorize_path(:google_oauth2) %></li>
          </ul>
      </li>
    </ul>
  </div>
</nav>
