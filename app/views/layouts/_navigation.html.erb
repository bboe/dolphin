<nav class="navbar navbar-expand navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand no-pad" href="#"><%= image_tag('dolphin.png', class: 'inline') %><%= Rails.configuration.app_title %></a>

    <ul class="navbar-nav ms-auto align-items-center">
      <li class="nav-item"><span class="navbar-text me-2"><%= display_user(current_user) %></span></li>
      <li class="nav-item"><%= button_to "Re-Authenticate", user_google_oauth2_omniauth_authorize_path, method: :post, class: "btn btn-link no-pad" %></li>
    </ul>
  </div>
</nav>
